@extends('admin.layouts.app')
@section('content')
<div class="admin-page-head"><div><span class="admin-kicker">Catalogue</span><h1>Catégories</h1><p>Gérez les familles de produits, leurs images et leur visibilité.</p></div><a href="{{ route('admin.categories.create') }}" class="btn btn-success"><i class="bi bi-plus-lg"></i> Nouvelle catégorie</a></div>
<div class="admin-stat-grid mb-4">@foreach([['Catégories',$stats['total'],'bi-diagram-3','text-success'],['Actives',$stats['actives'],'bi-check-circle','text-primary'],['Inactives',$stats['inactives'],'bi-pause-circle','text-warning'],['Avec produits',$stats['products'],'bi-box-seam','text-danger']] as [$label,$value,$icon,$color])<div class="admin-stat-card"><i class="bi {{ $icon }} {{ $color }}"></i><div><small>{{ $label }}</small><strong>{{ $value }}</strong></div></div>@endforeach</div>
<form class="card p-3 mb-4" method="get"><div class="row g-2 align-items-end"><div class="col-md-6"><label class="form-label small fw-bold" for="category-search">Recherche</label><input id="category-search" name="search" class="form-control" value="{{ request('search') }}" placeholder="Nom de catégorie"></div><div class="col-md-3"><label class="form-label small fw-bold" for="category-status">Statut</label><select id="category-status" name="statut" class="form-select"><option value="">Tous les statuts</option><option value="actif" @selected(request('statut')==='actif')>Active</option><option value="inactif" @selected(request('statut')==='inactif')>Inactive</option></select></div><div class="col-auto"><button class="btn btn-success"><i class="bi bi-funnel"></i> Filtrer</button></div><div class="col-auto"><a class="btn btn-outline-secondary" href="{{ route('admin.categories.index') }}"><i class="bi bi-arrow-counterclockwise"></i></a></div></div></form>
<div class="card border-0 shadow-sm"><div class="table-responsive"><table class="table align-middle mb-0"><thead><tr><th>Catégorie</th><th>Articles</th><th>Ordre</th><th>Statut</th><th class="text-end">Actions</th></tr></thead><tbody>@forelse($items as $category)<tr><td><div class="container-cell"><div class="container-thumb">@if($category->image_url)<img src="{{ asset($category->image_url) }}" alt="">@else<i class="bi bi-tag"></i>@endif</div><div><strong>{{ $category->nom }}</strong><small>{{ \Illuminate\Support\Str::limit($category->description,70) }}</small></div></div></td><td>{{ $category->containers_count }}</td><td>{{ $category->ordre_affichage }}</td><td><span class="status-pill {{ $category->statut==='actif'?'':'status-maintenance' }}">{{ ucfirst($category->statut) }}</span></td><td class="text-end"><div class="d-inline-flex gap-1"><form method="post" action="{{ route('admin.categories.toggle',$category) }}">@csrf<button class="btn btn-sm btn-outline-warning" title="{{ $category->statut==='actif'?'Désactiver':'Activer' }}"><i class="bi {{ $category->statut==='actif'?'bi-pause':'bi-play' }}"></i></button></form><a class="btn btn-sm btn-outline-success" href="{{ route('admin.categories.edit',$category) }}" title="Modifier"><i class="bi bi-pencil-square"></i></a><form method="post" action="{{ route('admin.categories.destroy',$category) }}" onsubmit="return confirm('Supprimer cette catégorie si elle ne contient aucun produit ?')">@csrf @method('DELETE')<button class="btn btn-sm btn-outline-danger" title="Supprimer"><i class="bi bi-trash"></i></button></form></div></td></tr>@empty<tr><td colspan="5" class="text-center py-5 text-muted">Aucune catégorie.</td></tr>@endforelse</tbody></table></div><div class="card-footer bg-white">{{ $items->links() }}</div></div>
@endsection
