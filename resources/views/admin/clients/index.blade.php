@extends('admin.layouts.app')
@section('content')
<div class="admin-page-head"><div><span class="admin-kicker">Relation client</span><h1>Clients</h1><p>Retrouvez les coordonnées, les entreprises et l’historique d’achats.</p></div><a href="{{ route('admin.clients.create') }}" class="btn btn-success"><i class="bi bi-person-plus"></i> Ajouter un client</a></div>
<div class="admin-stat-grid mb-4">@foreach([['Clients',$stats['total'],'bi-people','text-success'],['Particuliers',$stats['particuliers'],'bi-person','text-primary'],['Entreprises',$stats['entreprises'],'bi-building','text-warning'],['Commandes',$stats['commandes'],'bi-receipt','text-danger']] as [$label,$value,$icon,$color])<div class="admin-stat-card"><i class="bi {{ $icon }} {{ $color }}"></i><div><small>{{ $label }}</small><strong>{{ $value }}</strong></div></div>@endforeach</div>
<form class="card p-3 mb-4" method="get"><div class="row g-2 align-items-end"><div class="col-md-6"><label for="client-search" class="form-label small fw-bold">Recherche</label><input id="client-search" class="form-control" name="search" value="{{ request('search') }}" placeholder="Nom, entreprise, e-mail ou téléphone"></div><div class="col-md-3"><label for="client-type" class="form-label small fw-bold">Type de client</label><select id="client-type" class="form-select" name="type_client"><option value="">Tous</option><option value="particulier" @selected(request('type_client')==='particulier')>Particulier</option><option value="entreprise" @selected(request('type_client')==='entreprise')>Entreprise</option></select></div><div class="col-auto"><button class="btn btn-success"><i class="bi bi-search"></i> Rechercher</button></div><div class="col-auto"><a class="btn btn-outline-secondary" href="{{ route('admin.clients.index') }}"><i class="bi bi-arrow-counterclockwise"></i></a></div></div></form>
<div class="card border-0 shadow-sm"><div class="table-responsive"><table class="table align-middle mb-0"><thead><tr><th>Client</th><th>Type</th><th>E-mail</th><th>Téléphone</th><th>Localité</th><th class="text-end">Actions</th></tr></thead><tbody>@forelse($items as $client)<tr><td><strong>{{ trim(($client->prenom??'').' '.$client->nom) }}</strong>@if($client->nom_entreprise)<small class="d-block text-muted">{{ $client->nom_entreprise }}</small>@endif</td><td><span class="status-pill">{{ ucfirst($client->type_client ?? 'client') }}</span></td><td><a href="mailto:{{ $client->email }}">{{ $client->email }}</a></td><td>{{ $client->telephone ?: '—' }}</td><td>{{ collect([$client->code_postal,$client->ville,$client->pays])->filter()->join(', ') ?: '—' }}</td><td class="text-end"><div class="d-inline-flex gap-1"><a class="btn btn-sm btn-outline-secondary" href="{{ route('admin.clients.show',$client) }}" title="Voir"><i class="bi bi-eye"></i></a><a class="btn btn-sm btn-outline-success" href="{{ route('admin.clients.edit',$client) }}" title="Modifier"><i class="bi bi-pencil-square"></i></a></div></td></tr>@empty<tr><td colspan="6" class="text-center py-5 text-muted">Aucun client trouvé.</td></tr>@endforelse</tbody></table></div><div class="card-footer bg-white">{{ $items->links() }}</div></div>
@endsection
