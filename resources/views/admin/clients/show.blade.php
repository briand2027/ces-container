@extends('admin.layouts.app')
@section('content')
<div class="admin-page-head"><div><span class="admin-kicker">Relation client / Fiche</span><h1>{{ trim(($client->prenom??'').' '.$client->nom) }}</h1><p>{{ $client->nom_entreprise ?: ucfirst($client->type_client ?? 'client') }}</p></div><div class="d-flex gap-2"><a href="{{ route('admin.clients.index') }}" class="btn btn-outline-secondary"><i class="bi bi-arrow-left"></i> Retour</a><a href="{{ route('admin.clients.edit',$client) }}" class="btn btn-success"><i class="bi bi-pencil-square"></i> Modifier</a></div></div>
<div class="row g-4"><div class="col-xl-4"><section class="card border-0 shadow-sm h-100"><div class="card-header"><strong>Coordonnées</strong></div><div class="card-body"><dl class="row mb-0"><dt class="col-sm-5">Type</dt><dd class="col-sm-7">{{ ucfirst($client->type_client??'—') }}</dd><dt class="col-sm-5">Entreprise</dt><dd class="col-sm-7">{{ $client->nom_entreprise ?: '—' }}</dd><dt class="col-sm-5">E-mail</dt><dd class="col-sm-7"><a href="mailto:{{ $client->email }}">{{ $client->email }}</a></dd><dt class="col-sm-5">Téléphone</dt><dd class="col-sm-7">{{ $client->telephone ?: '—' }}</dd><dt class="col-sm-5">Adresse</dt><dd class="col-sm-7">{!! nl2br(e(collect([$client->adresse,$client->code_postal.' '.$client->ville,$client->pays])->filter()->join("\n"))) ?: '—' !!}</dd><dt class="col-sm-5">N° TVA</dt><dd class="col-sm-7">{{ $client->numero_tva ?: '—' }}</dd><dt class="col-sm-5">Inscription</dt><dd class="col-sm-7">{{ optional($client->created_at)->format('d/m/Y') }}</dd></dl>@if($client->notes)<hr><h3 class="h6">Notes internes</h3><p class="mb-0">{{ $client->notes }}</p>@endif @if($client->orders->isEmpty())<hr><form method="post" action="{{ route('admin.clients.destroy',$client) }}" onsubmit="return confirm('Supprimer cette fiche client ?')">@csrf @method('DELETE')<button class="btn btn-outline-danger btn-sm"><i class="bi bi-trash me-1"></i>Supprimer la fiche</button></form>@endif</div></section></div>
<div class="col-xl-8"><section class="card border-0 shadow-sm"><div class="card-header d-flex justify-content-between"><strong>Historique des commandes</strong><span class="badge text-bg-light border">{{ $client->orders->count() }}</span></div><div class="table-responsive"><table class="table align-middle mb-0"><thead><tr><th>Commande</th><th>Date</th><th>Statut</th><th>Paiement</th><th>Total</th></tr></thead><tbody>@forelse($client->orders as $order)<tr><td><a class="fw-semibold" href="{{ route('admin.orders.show',$order) }}">{{ $order->numero_commande }}</a></td><td>{{ optional($order->created_at)->format('d/m/Y') }}</td><td>{{ str_replace('_',' ',ucfirst($order->statut_commande)) }}</td><td>{{ ucfirst($order->statut_paiement) }}</td><td class="fw-bold">{{ number_format($order->total_ttc,2,',',' ') }} €</td></tr>@empty<tr><td colspan="5" class="text-center py-5 text-muted">Aucune commande associée.</td></tr>@endforelse</tbody></table></div></section></div></div>
@endsection
