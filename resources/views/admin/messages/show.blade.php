@extends('admin.layouts.app')
@section('content')
<div class="admin-page-head"><div><span class="admin-kicker">Messages / Détail</span><h1>{{ $message->sujet }}</h1><p>Reçu le {{ optional($message->date_creation)->format('d/m/Y à H:i') }}</p></div><div class="d-flex flex-wrap gap-2"><a href="{{ route('admin.messages.index') }}" class="btn btn-outline-secondary"><i class="bi bi-arrow-left"></i> Retour</a><form method="post" action="{{ route('admin.messages.destroy',$message) }}" onsubmit="return confirm('Supprimer ce message ?')">@csrf @method('DELETE')<button class="btn btn-outline-danger"><i class="bi bi-trash"></i></button></form></div></div>
<div class="row g-4"><div class="col-xl-7"><section class="card border-0 shadow-sm"><div class="card-header d-flex justify-content-between align-items-center"><strong>Message du client</strong><span class="status-pill {{ $message->statut==='non_lu'?'status-unread':($message->statut==='repondu'?'status-replied':'') }}">{{ $message->statut==='non_lu'?'Non lu':($message->statut==='repondu'?'Répondu':'Lu') }}</span></div><div class="card-body"><div class="message-sender"><div class="message-avatar">{{ mb_strtoupper(mb_substr($message->prenom ?: $message->nom,0,1)) }}</div><div><strong>{{ $message->prenom }} {{ $message->nom }}</strong><a href="mailto:{{ $message->email }}">{{ $message->email }}</a>@if($message->telephone)<a href="tel:{{ $message->telephone }}">{{ $message->telephone }}</a>@endif</div></div><hr><div class="message-body">{!! nl2br(e($message->message)) !!}</div></div></section>
@if($message->reponse)<section class="card border-0 shadow-sm mt-4"><div class="card-header"><strong>Réponse envoyée</strong><small class="d-block text-muted">{{ optional($message->date_reponse)->format('d/m/Y à H:i') }}</small></div><div class="card-body message-body">{!! nl2br(e($message->reponse)) !!}</div></section>@endif
</div><div class="col-xl-5"><section class="card border-0 shadow-sm"><div class="card-header"><h2 class="h5 mb-0"><i class="bi bi-reply text-success me-2"></i>Répondre par e-mail</h2></div><div class="card-body"><div class="small text-muted mb-3">À : <strong>{{ $message->email }}</strong><br>De : adresse principale définie dans les paramètres</div><form method="post" action="{{ route('admin.messages.reply',$message) }}">@csrf<label for="reponse" class="form-label">Votre réponse</label><textarea id="reponse" name="reponse" class="form-control" rows="10" maxlength="5000" required>{{ old('reponse',$message->reponse) }}</textarea><button class="btn btn-success w-100 mt-3"><i class="bi bi-send me-1"></i>Envoyer la réponse</button></form></div></section></div></div>
@endsection
