@extends('admin.layouts.app')
@section('content')
<div class="admin-page-head"><div><span class="admin-kicker">Offres et prestations</span><h1>Services</h1><p>Configurez les prestations, leurs tarifs et les conditions associées.</p></div><a href="{{ route('admin.services.create') }}" class="btn btn-success"><i class="bi bi-plus-lg"></i> Nouveau service</a></div>
<div class="admin-stat-grid mb-4">@foreach([['Services',$stats['total'],'bi-tools','text-success'],['Actifs',$stats['actifs'],'bi-check-circle','text-primary'],['Inactifs',$stats['inactifs'],'bi-pause-circle','text-warning'],['Avec tarif',$stats['tarifes'],'bi-currency-euro','text-danger']] as [$label,$value,$icon,$color])<div class="admin-stat-card"><i class="bi {{ $icon }} {{ $color }}"></i><div><small>{{ $label }}</small><strong>{{ $value }}</strong></div></div>@endforeach</div>
<form class="card p-3 mb-4" method="get"><div class="row g-2 align-items-end"><div class="col-md-6"><label class="form-label small fw-bold" for="service-search">Recherche</label><input id="service-search" name="search" class="form-control" value="{{ request('search') }}" placeholder="Nom ou catégorie de service"></div><div class="col-md-3"><label class="form-label small fw-bold" for="service-status">Statut</label><select id="service-status" name="statut" class="form-select"><option value="">Tous</option><option value="actif" @selected(request('statut')==='actif')>Actif</option><option value="inactif" @selected(request('statut')==='inactif')>Inactif</option></select></div><div class="col-auto"><button class="btn btn-success"><i class="bi bi-funnel"></i> Filtrer</button></div><div class="col-auto"><a class="btn btn-outline-secondary" href="{{ route('admin.services.index') }}"><i class="bi bi-arrow-counterclockwise"></i></a></div></div></form>
<div class="card border-0 shadow-sm"><div class="table-responsive"><table class="table align-middle mb-0"><thead><tr><th>Service</th><th>Type</th><th>Tarif</th><th>Unité</th><th>Statut</th><th class="text-end">Actions</th></tr></thead><tbody>@forelse($items as $service)<tr><td><div class="container-cell"><div class="container-thumb">@if($service->image_url)<img src="{{ asset($service->image_url) }}" alt="">@else<i class="bi bi-tools"></i>@endif</div><div><strong>{{ $service->nom }}</strong><small>{{ \Illuminate\Support\Str::limit($service->description,80) }}</small></div></div></td><td>{{ ucfirst(str_replace('_',' ',$service->type_service??'—')) }}</td><td class="fw-bold">{{ $service->prix_unitaire!==null?number_format((float)$service->prix_unitaire,2,',',' ').' €':'Sur demande' }}</td><td>{{ $service->unite_mesure ?: '—' }}</td><td><span class="status-pill {{ $service->statut==='actif'?'':'status-maintenance' }}">{{ ucfirst($service->statut) }}</span></td><td class="text-end"><div class="d-inline-flex gap-1"><a class="btn btn-sm btn-outline-success" href="{{ route('admin.services.edit',$service) }}" title="Modifier"><i class="bi bi-pencil-square"></i></a><form method="post" action="{{ route('admin.services.destroy',$service) }}" onsubmit="return confirm('Supprimer ou désactiver ce service ?')">@csrf @method('DELETE')<button class="btn btn-sm btn-outline-danger" title="Supprimer"><i class="bi bi-trash"></i></button></form></div></td></tr>@empty<tr><td colspan="6" class="text-center py-5 text-muted">Aucun service.</td></tr>@endforelse</tbody></table></div><div class="card-footer bg-white">{{ $items->links() }}</div></div>
@endsection
